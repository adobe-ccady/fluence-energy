{
  "definitions": [
    {
      "title": "Columns",
      "id": "columns",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Columns",
              "model": "columns",
              "filter": "columns"
            }
          }
        }
      }
    },
    {
      "title": "Column",
      "id": "column",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block/item",
            "template": {
              "name": "Column",
              "model": "column"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "columns",
      "fields": [
        {
          "component": "select",
          "valueType": "string",
          "name": "classes",
          "label": "Variant",
          "description": "Layout / styling variant for this columns block.",
          "options": [
            { "name": "Split (image + content, 50/50)", "value": "columns-split" },
            { "name": "About (text + rounded image)", "value": "columns-about" },
            { "name": "Promo (icon + heading + text grid)", "value": "columns-promo" },
            { "name": "Pull quote", "value": "columns-pullquote" },
            { "name": "Call to action", "value": "columns-cta" }
          ]
        }
      ]
    },
    {
      "id": "column",
      "fields": [
        {
          "component": "richtext",
          "valueType": "string",
          "name": "text",
          "label": "Content",
          "description": "Heading, paragraphs, links, or a single image for this column."
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "columns",
      "components": [
        "column"
      ]
    }
  ]
}
