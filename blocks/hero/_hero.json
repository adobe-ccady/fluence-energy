{
  "definitions": [
    {
      "title": "Hero",
      "id": "hero",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Hero",
              "model": "hero",
              "filter": "hero"
            }
          }
        }
      }
    },
    {
      "title": "Hero Slide",
      "id": "hero-slide",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block/item",
            "template": {
              "name": "Slide",
              "model": "hero-slide"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "hero",
      "fields": [
        {
          "component": "select",
          "valueType": "string",
          "name": "classes",
          "label": "Variant",
          "description": "Full-bleed overlay hero, or a rotating carousel with a background video.",
          "options": [
            { "name": "Default (image + content)", "value": "" },
            { "name": "Carousel (video background + slides)", "value": "hero-carousel" }
          ]
        },
        {
          "component": "reference",
          "valueType": "string",
          "name": "image",
          "label": "Background / hero image",
          "description": "Poster image. For the carousel variant this is the video poster."
        },
        {
          "component": "text",
          "valueType": "string",
          "name": "imageAlt",
          "label": "Image alt text"
        },
        {
          "component": "richtext",
          "valueType": "string",
          "name": "text",
          "label": "Content",
          "description": "Eyebrow, heading, and optional supporting text for the default (non-carousel) hero."
        },
        {
          "component": "aem-content",
          "valueType": "string",
          "name": "video",
          "label": "Background video (carousel only)",
          "description": "Link to a .mp4/.webm/.ogg used as the autoplaying background."
        }
      ]
    },
    {
      "id": "hero-slide",
      "fields": [
        {
          "component": "richtext",
          "valueType": "string",
          "name": "text",
          "label": "Heading & body",
          "description": "Optional eyebrow, slide heading, and optional supporting paragraph."
        },
        {
          "component": "aem-content",
          "valueType": "string",
          "name": "ctaLink",
          "label": "CTA link"
        },
        {
          "component": "text",
          "valueType": "string",
          "name": "ctaLinkText",
          "label": "CTA label"
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "hero",
      "components": [
        "hero-slide"
      ]
    }
  ]
}
